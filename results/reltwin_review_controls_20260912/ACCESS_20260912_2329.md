# 2026-09-12 23:29 续办：SSH 连接被拒绝

本次续办读取了执行说明和 Git 状态。之前认证的本地 RPC 会话已不存在，重新连接最新授权的 `connect.westc.seetacloud.com:56527` 失败；Paramiko 报端口无法连接。随后 DNS 正常解析到 `116.172.66.186`，两次独立 TCP 连接检查均返回 `ConnectionRefusedError / WinError 10061`。拒绝发生在 SSH 认证之前，不是密码认证失败，也不能据此判断训练本身报错。

最后一次成功远端检查时，队列已完成 44/50 个步骤，六次新训练和两组三种子核心对照均完成，正在进行 `official/relation`。之后还需 official/SFT 两个基线的关系和公开集复核。本次无法核实这些尾部任务是否已结束，不能把上次进度当作当前完成状态。

此前三种子去交换项完整预测已取回并提交；等更新步数对照的最终逐行输出尚未完整取回，本地只有中间快照。此前只读检查得到 SetPO 相对 continuation 的三种子关系 mIoU 增量约 +0.769174 点，但未完成最终配对置信区间及完整归档，不能据此扩大发表结论。

本次未启动重复队列、未改冻结源码或参数、未修改基线和论文数值。下一步先恢复原端口访问，或由用户提供重启后的新连接；恢复后检查实际状态、进程和完整文件，再继续最终审计与论文修订。服务器端训练已脱离原 SSH，因此本地会话失效本身不能说明远端任务被停止。

自动续办仍保留，未完成交付前不将任务记为完成。同一连接问题持续时不重复通知；只有连接恢复、出现新的异常或需要新的访问地址时更新。
