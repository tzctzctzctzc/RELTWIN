# 论文仓库协作规则

根据作者 2026-09-14 的最新要求，本目录内的论文维护采用以下流程，替代上级目录“每次修改必须新建分支”在本论文仓库中的约定；其他实验仓库不受影响。

1. 直接维护 `paper` 分支。完成用户授权的论文修改后保存 Git 提交并推送，不再将独立审阅分支作为交付终点或等待额外合并确认。
2. 开始修改前检查工作区并获取远端 `paper` 更新，保留用户和协作者的修改。存在分歧时正常合并，不强推、不重置历史、不用本地整文件覆盖远端更新。
3. 遇到冲突，结合两侧提交逐项解决。实验数值、作者信息或互斥的研究结论若无法由现有证据确定，应核实或询问，不能猜测。
4. 论文改动同步维护 LaTeX 源码、固定名称的 `paper.pdf` 和 `ISSUES.md`。源码合并后重新编译并检查四页正文、第五页声明与参考文献、引用及版面；PDF 冲突从合并后的源码重新生成。
5. 只改问题清单或协作记录时注明 PDF 未变。不要增加平行 final PDF，不提交编译缓存。
6. 推送后核验本地 `paper` 与远端一致；若推送因协作者新提交被拒，重新获取、合并、验证后再推送。
