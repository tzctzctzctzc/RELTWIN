\begin{figure}[H]
\centering
\includegraphics[width=\columnwidth]{figures/pair_recovery.pdf}
\caption{RelTwin recovers query--window binding failures hidden by aggregate scores. Each ribbon tracks the same inverse-query pair from SFT to RelTwin at the 0.5 joint-IoU threshold ($N=160$); 51 pairs are recovered while one regresses. The bottom annotation reports exact same-window predictions for the two inverse queries.}
\label{fig:recovery}
\end{figure}
