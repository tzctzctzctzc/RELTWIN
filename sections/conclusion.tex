\section{Conclusion}
NOVA combines relation-aware candidate generation, verification, and selective refinement for audio grounding. RelTwin's locally valid timestamp contrasts train the choice between acoustically supported windows. NOVA benchmark results and RelTwin's matched training and timing analyses demonstrate grounding performance and query-specific selection. The key is to supervise which real occurrence answers the query, alongside event presence.
